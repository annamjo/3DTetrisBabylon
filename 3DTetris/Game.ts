/*import {GameBoard} from './GameBoard.js';*/

class Game {
    // private _scene: BABYLON.Scene;

    constructor() {
        // create canvas, scene (has gameboard), engine?
    }

    // createScene() {} //animation loop

    drawBlock() { //loop - draw another block based on time interval/when current block not active
        //randomize block - array of options (string or number); spawn a random block
    } //draw drop preview?
    
    getNextBlock() {} //also preview next block

    removeBlocks() {} //when layer cleared

    collpaseBlocks() {} //when gameboard layers (spaces array) collapse
}